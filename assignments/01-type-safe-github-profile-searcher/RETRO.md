# Retro

## 잘된 점

- 맨 처음 하나의 파일에서 구현하는 것

## 아쉬운 점

- 파일을 쪼개는 과정에서 매끄럽지 않았음.
- 타입 좁히는 것에 대한 연습이 더 필요하다고 느꼈음.

## 다음 과제 액션 아이템

- 구현 전에 “필수 범위(타입 분리, API 분리, 에러 분기)”를 먼저 확정하고 시작한다.
- 타입은 기본적으로 `named export + import type` 규칙으로 통일한다.
